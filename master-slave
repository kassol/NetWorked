master-slave:
1.MT_MASTER(NULL)
2.MT_METAFILE(METAFILE_NAME, SIZE)
3.MT_FILE(FILE_NAME, SIZE)
4.MT_FILE_READY(PORT)/MT_FILE_FAIL(MSG)

slave-master:
1.MT_FAIL(IP_ADDRESS)/MT_SUCCESS(NULL)
2.MT_METAFILE_READY(PORT)/MT_METAFILE_FAIL(MSG)
3.MT_FILE_READY(PORT)/MT_FILE_FAIL(MSG)
4.MT_FILE(FILE_NAME, SIZE)

task-state:
0:未分配
1:已分配
2:已发送
3:已完成

MetaFile->FileList->MT_FILE_REQUEST(FILE_NAME)->MT_FILE(FILE_NAME, SIZE)->MT_FILE_READY(PORT, FILE_NAME)
